# API reference

> TODO (finalize locally): auto-generated reference via mkdocstrings. This imports
> `sigway` (needs jax etc.), so it is wired up and built in the local session,
> not in the unattended cloud run.
