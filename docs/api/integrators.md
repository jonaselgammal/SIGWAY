# sigway.integrators

The integrator performs the double integral over the two internal momenta $s\in[0,1]$ and
$t\in[0,\infty)$.

## Base class

::: sigway.integrators.Integrator
    options:
      heading_level: 3

## Simpson quadrature

::: sigway.integrators.SimpsonIntegrator
    options:
      heading_level: 3
